/** Editorial / MD3-style typography utility strings — use with `className={cn(EYEBROW, …)}`. */

/** Primary eyebrow / kicker (10px uppercase gold). */
export const EYEBROW = 'text-[10px] uppercase tracking-[0.2em] text-primary font-bold';

/** Large italic headline (Newsreader). */
export const HERO_ITALIC =
  'font-headline italic text-5xl text-on-surface tracking-tight';

/** Section subtitle / lead paragraph. */
export const BODY_LEAD = 'text-on-surface-variant text-lg mt-2 max-w-xl';

/** Tiny uppercase label (wider tracking). */
export const LABEL_TINY = 'text-[10px] uppercase tracking-widest text-primary font-bold';
