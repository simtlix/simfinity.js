// Custom mutations - search and slot-availability have been removed in favor
// of standard Simfinity find()/where() queries driven by the UI.
// Only keep this file for future business-logic mutations that cannot be
// expressed as simple CRUD + scopes.
