---
description: Documents the field/type extensions metadata system used in GraphQLObjectType definitions
globs: packages/core/src/**, packages/mongodb/src/mongo/**, packages/postgres/src/schema/**, docs/reference/extensions.md
alwaysApply: false
---

# Extensions Metadata System

Simfinity uses the standard GraphQL `extensions` property on fields and types to carry metadata that drives schema generation, model creation, resolver wiring, and validation.

## Introspection Initialization

At import time, Simfinity adds an `extensions` field through `__Field.getFields()` if one is not already present. It preserves the existing field map and metadata type identity across repeated module evaluation with a shared GraphQL peer, including after fields or schemas have been materialized. The resolver returns the source field's original extensions; introspection exposes only `relation`, `stateMachine`, and `readOnly`, without removing arbitrary application metadata.

This is still a global GraphQL extension. Schemas constructed afterward register `FieldExtensionsType` and `RelationType`; earlier schemas retain their original type maps and cannot validate named fragments on those new types. Schema cloning and `buildClientSchema()` roundtrips in a process that imported Simfinity remain unsupported; see the architecture rule.

## Field-Level Extensions

### `extensions.relation`

Defines relationships between types. Required on every field whose type is another `GraphQLObjectType` or `GraphQLList` of one.

```javascript
fields: () => ({
  author: {
    type: AuthorType,
    extensions: {
      relation: {
        embedded: false,          // reference: ObjectId on MongoDB, UUID/FK on PostgreSQL
        connectionField: 'author_id',  // stored reference field (defaults to field name)
        displayField: 'name',     // field shown in introspection UI hints
      },
    },
  },
  chapters: {
    type: new GraphQLList(ChapterType),
    extensions: {
      relation: {
        embedded: true,           // value owned by its parent
      },
    },
  },
})
```

- **Non-embedded** (`embedded: false`): single references use ObjectId on MongoDB and UUID columns with real FKs on PostgreSQL. Inputs use `IdInputType` (`{ id: String! }`); collections generate `OneToMany` inputs with `added`/`updated`/`deleted` sub-fields and resolve through the selected adapter.
- **Embedded** (`embedded: true`): MongoDB stores nested documents inline. PostgreSQL stores reference-free values as JSONB and reference-bearing values in private owned tables with FKs. Both expose nested GraphQL inputs and parent-owned lifecycle semantics.
- **`connectionField`**: the stored field name for a single reference. If omitted, defaults to the GraphQL field name consistently during model generation, reads, writes and nullable update clears. For referenced collections, explicitly configure the child's back-reference field.
- **Non-null wrappers**: relation objects and lists retain outer non-null requirements in create inputs; updates remove the outer wrapper. Embedded list items and referenced `added`/`updated` items preserve item nullability.

### `extensions.readOnly`

```javascript
createdAt: {
  type: GraphQLString,
  extensions: { readOnly: true },
}
```

Excludes the field from both add and update input types. The field can only be set programmatically (e.g., in a controller hook).

### `extensions.unique`

```javascript
email: {
  type: GraphQLString,
  extensions: { unique: true },
}
```

Adds a backend uniqueness structure for supported mappings. MongoDB's generator honors it on string/enum and numeric scalar fields. PostgreSQL supports mapped scalars, single references, scalar lists and scalar/reference leaves in embedded values; see the storage details below. Do not describe every MongoDB field mapping as supporting this flag.

### `extensions.validations`

```javascript
name: {
  type: new GraphQLNonNull(GraphQLString),
  extensions: {
    validations: validators.stringLength('Name', 2, 100),
    // Returns: { CREATE: [validator], UPDATE: [validator] }
  },
}
```

Validators are executed during `materializeModel()` before saving/updating. Each validator has a `.validate(typeName, fieldName, value, session)` method. The `CREATE` array runs on add, `UPDATE` on update.

### `extensions.stateMachine` (auto-set)

Not set manually. When `buildInputType()` detects a `state` field on a type that has a state machine, it sets `extensions.stateMachine = true` on that field and excludes it from input types.

## Type-Level Extensions

### `extensions.indexes` (PostgreSQL)

Use `{ indexes: [{ fields: ['serie', 'star'], unique: true }] }` on a GraphQL type to declare a composite PostgreSQL index. Fields resolve to stored scalar/reference columns; list and embedded paths are rejected. MongoDB still uses its existing index/onModelCreated mechanism. PostgreSQL root scalar/single-reference `unique` uses `NULLS NOT DISTINCT`. Scalar/reference unique fields within embedded trees and declared unique scalar lists use private native typed key tables, one key per root/value, with distinct null and terminal-empty states. Missing/null/empty embedded ancestors contribute null keys. Generated deferred triggers refresh keys and enforce owned shape after sequential nested writes; immediate owner-version writes prevent same-owner concurrency write skew. Value-only JSONB shapes use immutable generated validators and CHECKs. Whole embedded-object uniqueness remains unsupported. Mongo scalar-list flags remain ignored; its generated unique scalar string/enum/numeric indexes retain existing behavior.

PostgreSQL metadata requires a child `connectionField` for inverse collections. A singular reference defaults to its GraphQL field name. Reciprocal collections do not create implicit many-to-many storage: use an explicit linking entity with references to both sides.

### `extensions.validations` (on the type)

```javascript
const BookType = new GraphQLObjectType({
  name: 'Book',
  extensions: {
    validations: {
      CREATE: [{ validate: async (typeName, args, modelArgs, session) => { /* cross-field validation */ } }],
      UPDATE: [{ validate: async (typeName, args, modelArgs, session) => { /* ... */ } }],
    },
  },
  fields: () => ({ /* ... */ }),
});
```

Type-level validators run after all field-level validators, receiving the full args and materialized model.

### `extensions.scope`

```javascript
const BookType = new GraphQLObjectType({
  name: 'Book',
  extensions: {
    scope: {
      find: async ({ type, args, operation, context }) => {
        args.owner_id = { operator: 'EQ', value: context.user.id };
      },
      get_by_id: async ({ type, args, operation, context }) => { /* ... */ },
      aggregate: async ({ type, args, operation, context }) => { /* ... */ },
    },
  },
  fields: () => ({ /* ... */ }),
});
```

Scope functions modify query args in-place to enforce row-level security. The shared runtime calls `executeScope()` before compiling the MongoDB pipeline or PostgreSQL query. Flat scope predicates remain conjoined with caller logical groups; generated non-embedded relationships run target scopes with immutable identity/parent predicates.
